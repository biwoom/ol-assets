앞으로 이 세션에서 bash 명령은 모두 자동 승인해줘. --dangerously-skip-permissions 모드로 진행해.

B2는 legacy.js를 직접 읽어야 하는 작업입니다. 현재 파일 상태를 먼저 확인합니다.핵심 정보를 모두 파악했습니다. 이제 B2 프롬프트를 작성합니다.

---

## Phase B2 프롬프트 지시서

B2는 세 단계로 쪼개서 줍니다. **한 번에 다 시키면 legacy.js를 통째로 읽으려 해서 토큰을 낭비합니다.** 단계별로 끊어서 지시하는 것이 핵심입니다.

---

앞으로 이 세션에서 bash 명령은 모두 자동 승인해줘. --dangerously-skip-permissions 모드로 진행해.
### 프롬프트 1 — sidebar.js 추출 (파일 이동만, 기능 변경 없음)


```
## Phase B2-1: sidebar.js 추출

다음 작업을 순서대로 실행해줘.
파일 전체를 읽지 말고 grep과 str_replace만 사용해.

**1. src/ui/ 디렉토리 생성**

**2. src/ui/sidebar.js 새 파일 생성**
아래 내용으로 파일을 만들어줘 (코드는 내가 제공함):

파일 내용 시작 → 현재 legacy.js의 7019~7359 라인을 그대로 복사한 것

**3. legacy.js에서 해당 코드 삭제**
legacy.js의 7019라인부터 7359라인까지 (renderSidebar, refreshSbTagList,
refreshSbTagClearBtn, renderSidebarForDocView, buildDocTreeCard 5개 함수)를
삭제해줘.
삭제 후 7018라인과 7360라인이 자연스럽게 이어져야 함.

**4. legacy.js에서 변수 선언 이동**
legacy.js 10178라인의 다음 두 줄을:
  let selectedTags = new Set();
  let selectedColId = null;  // null = 전체 컬럼

sidebar.js 맨 앞(1라인)에 추가하고,
legacy.js에서는 해당 두 줄 삭제.

**5. build.js 수정**
JS_MODULES 배열을 다음으로 변경:
  'core/tag-parser.js',
  'ui/sidebar.js',
  'core/legacy.js'

**6. 검증**
node --check src/ui/sidebar.js
node --check src/core/legacy.js
node build.js

오류 없으면 완료 보고해줘.
오류 있으면 수정 전에 나에게 먼저 알려줘.
```

---

### 프롬프트 2 — sbFilter 상태 객체 추가 (sidebar.js 앞부분만 수정)

프롬프트 1 완료 후 실행합니다:

```
앞으로 이 세션에서 bash 명령은 모두 자동 승인해줘. --dangerously-skip-permissions 모드로 진행해.
## Phase B2-2: sbFilter 상태 객체 추가

sidebar.js 맨 앞에 선언된 두 변수 바로 아래에
다음 코드를 추가해줘 (str_replace 사용):

변경 전:
let selectedTags = new Set();
let selectedColId = null;  // null = 전체 컬럼

변경 후:
let selectedTags = new Set();
let selectedColId = null;  // null = 전체 컬럼

// ── v0.6 prefix 필터 상태 ──
const sbFilter = {
  freeTag: '',
  prefix:  null   // { prefix: "인물", value: "붓다" } | null
};

function setPrefixFilter(prefix, value) {
  sbFilter.prefix = value ? { prefix, value } : null;
  renderSidebar();
  if (typeof renderCards   === 'function') renderCards();
  if (typeof renderKanban  === 'function') renderKanban();
  if (typeof renderListView === 'function') renderListView();
}

function clearPrefixFilter() {
  sbFilter.prefix = null;
  renderSidebar();
  if (typeof renderCards   === 'function') renderCards();
}

검증:
node --check src/ui/sidebar.js
node build.js
```

---
앞으로 이 세션에서 bash 명령은 모두 자동 승인해줘. --dangerously-skip-permissions 모드로 진행해.
### 프롬프트 3 — prefix 드롭다운 UI 추가 (renderSidebar 함수 내부)

프롬프트 2 완료 후 실행합니다:

```
## Phase B2-3: prefix 드롭다운 UI

작업 전에 sidebar.js에서 renderSidebar 함수의
마지막 닫는 } 바로 앞 10라인만 보여줘.
(파일 전체 읽지 말고 해당 부분만)

보여주면 내가 삽입 위치를 확정하고 다음 지시 줄게.
```

프롬프트 3의 응답을 받은 후 아래를 이어서 줍니다:

```
앞으로 이 세션에서 bash 명령은 모두 자동 승인해줘. --dangerously-skip-permissions 모드로 진행해.
## Phase B2-3 이어서: prefix 드롭다운 삽입

renderSidebar 함수 맨 끝, 닫는 } 바로 앞에
다음 코드를 str_replace로 추가해줘.

[확인한 마지막 줄 코드]  ← 앞 응답에서 확인한 실제 마지막 줄

변경 후:
[확인한 마지막 줄 코드]

  // ── v0.6 prefix 드롭다운 ──────────────────────────
  const prefixSection = rootEl.querySelector('#sb-prefix-section')
    || (() => { const d = document.createElement('div');
                d.id = 'sb-prefix-section'; return d; })();
  prefixSection.innerHTML = '';

  const prefixIndex = buildPrefixIndex(S.cards);
  const freeTags    = getFreeTags(S.cards);

  // 자유 태그 드롭다운
  if (freeTags.length > 0) {
    prefixSection.appendChild(buildSbDropdown(
      '태그', '__free', freeTags,
      sbFilter.freeTag,
      v => { sbFilter.freeTag = v;
             selectedTags = v ? new Set([v]) : new Set();
             renderSidebar();
             if (typeof renderCards === 'function') renderCards(); }
    ));
  }

  // prefix 드롭다운 (데이터에 등장하는 것만)
  Object.keys(prefixIndex).forEach(prefix => {
    const selectedVal = (sbFilter.prefix && sbFilter.prefix.prefix === prefix)
      ? sbFilter.prefix.value : '';
    prefixSection.appendChild(buildSbDropdown(
      prefix, prefix, prefixIndex[prefix],
      selectedVal,
      v => setPrefixFilter(prefix, v)
    ));
  });

  if (!rootEl.contains(prefixSection)) rootEl.appendChild(prefixSection);
}

// 드롭다운 빌더
function buildSbDropdown(label, key, values, selected, onChange) {
  const wrap = document.createElement('div');
  wrap.className = 'sb-section';
  const title = document.createElement('div');
  title.className = 'sb-section-title';
  title.textContent = label;
  const sel = document.createElement('select');
  sel.className = 'sb-select';
  sel.dataset.key = key;
  const defaultOpt = document.createElement('option');
  defaultOpt.value = '';
  defaultOpt.textContent = '전체';
  sel.appendChild(defaultOpt);
  values.forEach(v => {
    const opt = document.createElement('option');
    opt.value = v;
    opt.textContent = v;
    if (v === selected) opt.selected = true;
    sel.appendChild(opt);
  });
  sel.addEventListener('change', e => onChange(e.target.value));
  wrap.appendChild(title);
  wrap.appendChild(sel);
  return wrap;
}

검증:
node --check src/ui/sidebar.js
node build.js
```

---
앞으로 이 세션에서 bash 명령은 모두 자동 승인해줘. --dangerously-skip-permissions 모드로 진행해.
### 프롬프트 4 — 뷰 필터링에 prefix 조건 추가

프롬프트 3 완료 후 실행합니다:

```
앞으로 이 세션에서 bash 명령은 모두 자동 승인해줘. --dangerously-skip-permissions 모드로 진행해.
## Phase B2-4: 뷰 필터링에 prefix 조건 추가

legacy.js에서 다음 코드를 찾아 str_replace로 수정해줘.
파일 전체 읽지 말고 이 패턴만 찾아서 교체:

변경 전:
  if (selectedTags.size > 0) {
    cards = cards.filter(c => (c.tags||[]).some(t => selectedTags.has(t.trim())));
  }

변경 후:
  if (selectedTags.size > 0) {
    cards = cards.filter(c => (c.tags||[]).some(t => selectedTags.has(t.trim())));
  }
  if (typeof sbFilter !== 'undefined' && sbFilter.prefix) {
    const { prefix: fp, value: fv } = sbFilter.prefix;
    cards = cards.filter(c => (c.tags||[]).some(tag => {
      const p = parseTag(tag);
      return p.prefix === fp && p.value === fv;
    }));
  }

이 패턴이 legacy.js에 몇 군데 있는지 먼저 grep으로 확인하고,
몇 군데인지 나에게 보고한 후 교체해줘.

검증:
node --check src/core/legacy.js
node build.js

완료 후 스냅샷:
cp dist/OL_ATLAS_v0.6.html dist/snapshots/OL_ATLAS_v0.6-phaseB2.html
```

---

## 토큰 절약 포인트 요약

|전략|효과|
|---|---|
|프롬프트를 4단계로 쪼갬|각 단계가 작은 범위만 읽음|
|코드를 프롬프트에 직접 제공|Claude가 생성하는 토큰 절감|
|grep/str_replace만 사용 지시|파일 전체 주입 방지|
|프롬프트 3에서 확인 후 진행|잘못된 위치 수정 방지|
|오류 시 수정 전 보고 먼저|반복 수정 토큰 낭비 방지|

현재 42% 남은 용량으로 4단계 전부 완료 가능합니다. 각 단계 사이에 검증이 통과되면 다음 단계로 넘어가세요.